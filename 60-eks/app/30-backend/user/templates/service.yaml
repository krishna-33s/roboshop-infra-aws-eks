apiVersion: v1
kind: Service
metadata:
  name: user
  namespace: roboshop
  labels:
    project: roboshop
    tier: backend
    component: user
    environment: dev
spec:
  selector:
      project: roboshop
      tier: backend
      component: user
      environment: dev

  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080

